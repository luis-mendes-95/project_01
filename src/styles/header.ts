import styled from "styled-components";

export const Header_app = styled.header`

    button {
        background-color: black;
        color: orange;
        padding: 5px;
        border-radius: 8px;
        margin: 2px 4px;
        cursor: pointer;
        transition: 0.3s;
    }

    button:hover{
        transform: scale(1.1);
        transition: 0.3s;
    }

`;
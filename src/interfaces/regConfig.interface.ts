export interface CurrencyInputProps {
    label: string;
    id: string;
  } //essa interface foi gerada pelo chatGpt, solicitei que fizesse uma tipagem para uma função de input que alterasse o valor para currency e essa tipagem foi o resultado...